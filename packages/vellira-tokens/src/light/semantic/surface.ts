import { colors } from '../../primitives/colors.js';

export const surface = {
  default: colors.gray[0],
  muted: colors.gray[55],
  subtle: colors.gray[100],
  elevated: colors.gray[0],

  inverse: colors.gray[900],
} as const;
